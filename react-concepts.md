# React Concept 1 - Lift State Up

When multiple components need the same data,
the state should live in their closest common parent.

Children receive the data through props.

Children ask the parent to update the data through callback functions.
